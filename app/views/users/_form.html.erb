<form id="user-form">
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label> Name </label>
    <input type="text" id="name"/>
  </div>
  <div class="field">
    <label> Email </label>
    <input type="email" id="email"/>
  </div>
  <div class="actions">
    <input type='button' id="new-user" value="Create User" />
  </div>
</form>
